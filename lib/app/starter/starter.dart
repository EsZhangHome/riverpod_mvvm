// Starter 占位组件的唯一公共入口。
//
// main.dart 只依赖这个文件，不直接引用页面、路径或 Mock 实现。真实项目接入自己的
// 路由与依赖组合后，替换这个 import 即可删除整个 app/starter 目录。

export 'starter_composition.dart' show buildStarterRoot;
export 'starter_route_bundle.dart' show createStarterRouteBundle;
