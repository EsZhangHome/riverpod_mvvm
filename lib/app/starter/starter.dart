// Starter 占位组件的唯一公共入口。
//
// main.dart 只依赖这个文件，不直接引用页面或路径。真实项目接入自己的路由包后，
// 替换 main.dart 的一个 import 和一个函数调用，就可以删除整个 app/starter 目录。

export 'starter_route_bundle.dart' show createStarterRouteBundle;
